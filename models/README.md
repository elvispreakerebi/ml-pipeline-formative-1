# Trained Models

Run `python task1/train_and_save_model.py` to generate `sales_model.pkl` for Task 4.

The model is a Random Forest (Task 1 tuned params) trained on Lag_1, Lag_7, MA_7, MA_30 features.
